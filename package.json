{
  "name": "tauri-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "relay": "cd server && npm install && npm start",
    "test": "vitest run",
    "test:cov": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@vitest/coverage-v8": "^2.1.9",
    "fastenhancer-web": "^0.1.1",
    "jsdom": "^24.1.0",
    "vitest": "^2.1.0"
  }
}
